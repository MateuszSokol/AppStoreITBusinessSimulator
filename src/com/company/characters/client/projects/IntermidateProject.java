package com.company.characters.client.projects;

import com.company.characters.client.Client;

public class IntermidateProject extends Project{
    public IntermidateProject(String projectName, ProjectType projectType, Integer workersAmount, Client client) {
        super(projectName, projectType, workersAmount, client);
    }
}
