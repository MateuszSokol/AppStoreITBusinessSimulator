package com.company.characters.client;

import com.company.characters.client.projects.Project;

import java.util.Date;

public class LazyClient extends Client{
    public LazyClient(String clientCompanyName, ClientTypes clientType) {
        super(clientCompanyName, clientType);
    }
}
